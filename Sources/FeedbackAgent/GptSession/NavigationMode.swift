import Foundation

/// Indicates whether a navigation map is being used without guidance (passive),
/// or a NavigationViewController is being used with active guidance (active).
public enum NavigationMode {
    case passive
    case active
}
